/// @description Step Logic

// Update depth each frame
depth = -y - 1;

//set_movement(direction_, speed_);
add_movement_maxspeed(direction_, 2, 2);
move_movement_entity(false);

if (speed_ = 0) instance_destroy();

