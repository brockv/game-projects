///// @description Step Logic

if (o_player.x < right_boundary_)
{
	if (o_player.y > top_boundary_ && o_player.y < bottom_boundary_)
	{
		//surface_set_target(outline_surf);
		//shader_set(sh_shift_to_clear);
		//var _alpha = approach(1.0, 0.0, 0.1);
		//shader_set_uniform_f(uniformID_, _alpha);
		//draw_surface(outline_surf, 0, 0);
		//shader_reset();
		//surface_reset_target();

	}
}


//surface_set_target(outline_surf);
//shader_set(sh_shift_to_full);
//draw_surface(outline_surf, 0, 0);
//shader_reset();
//surface_reset_target();


