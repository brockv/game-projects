/// @description Initialize the switch

// Inherit from o_switch_parent
event_inherited();

// Define the colors for this switch
active_color_ = make_color_rgb(255, 255, 0);
start_color_ = make_color_rgb(255, 255, 1);
color_ = start_color_;

