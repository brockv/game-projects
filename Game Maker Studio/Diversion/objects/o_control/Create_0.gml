/// @description Initialize variables

// Create the buttons for selecting the resolution
var _button = noone;

// Left arrow
//_button = instance_create_depth(0, 0, 0, o_option_arrow); _button.sprite_index = s_arrow_left;

// Right arrow
//_button = instance_create_depth(0, 0, 0, o_option_arrow); _button.sprite_index = s_arrow_right;

// Apply button
//instance_create_depth(0, 0, 0, o_apply);

// Full screen button
//instance_create_depth(0, 0, 0, o_fullscreen);

// -- Misc -- //

// Set the initial display to the current resolution
resolution_selected_ = global.resolution_id_;

