/// @description Insert description here
// You can write your code in this editor

//draw_self();

//var _string = id;
//var _distance = point_distance(x, y, x - (sprite_width / 2), y - (sprite_height / 2));

//draw_text(x - string_width(id) / 2, y - 20, id);
//draw_text(x - string_width(_string) / 2, y - 40, _distance);

//draw_rectangle(x - (width_ / 2), y - (height_ / 2), x + (width_ / 2), y + (height_ / 2), true);