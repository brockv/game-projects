/// @description Move particle

/*
// move sprite up
x -= player_dir;
y -= random_spd;

// rotate sprite
rotation += rotation_dir;

// reduce alpha and delete sprite
alpha -= 0.05;
if (alpha <= 0 ) instance_destroy();