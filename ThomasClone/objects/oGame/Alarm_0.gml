/// @description  Turn off screenshake

/*
shake     = false;
babyshake = false;

/* */
/// Give the camera to the player.

playerActive = true;

hDist   = 90; // How far away horizontally the camera should be
vDist   = 64;  // How far away vertically the camera should be
cam.xTo = oPlayer.x + hDist; // Camera's initial xTo position
cam.yTo = oPlayer.y - vDist; // Camera's initial yTo position
peek    = oPlayer.sprite_width + 1; // Distance to check if a player is looking over a ledge
camSpd  = 20; //How quickly the camera should move. The lower the value, the faster it is

/* */
/*  */
