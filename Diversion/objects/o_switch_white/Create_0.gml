/// @description Initialize the switch

// Inherit from o_switch_parent
event_inherited();

// Define the colors for this switch
active_color_ = make_color_rgb(255, 255, 255);
start_color_ = make_color_rgb(254, 254, 254);
color_ = start_color_;

