/// @description  Change from Static to Dynamic light.
// Example of changing a static light to dynamic, and back again.

// First define the shadow mesh

Aura_Shadow_Caster_Box_Init();

// Call an alarm to check for a collision with the "player".
alarm[0] = room_speed;

